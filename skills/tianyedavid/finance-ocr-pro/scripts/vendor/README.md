# Vendored Frontend Assets

This directory contains local copies of JavaScript files used by
`scripts/md_to_html.py`.

- Mermaid `11.14.0` from the `mermaid` npm package
- MathJax `3.2.2` from the `mathjax` npm package

These files are bundled to keep HTML generation self-contained and to avoid
runtime downloads from CDNs.
